@model IEnumerable<SecretViewLog>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.InitialViewDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ViewsCount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InitialViewDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ViewsCount)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ViewerMachineName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ViewerMachineName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ViewerMachineName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
